{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run migrate && npm run seed && node -e \"try { require('./src/index.js') } catch(e) { console.error('ERRO:', e.message, e.stack); process.exit(1) }\""
  }
}
